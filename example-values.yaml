---
image:
  # Replace with the immutable Git SHA tag you pushed to GHCR.
  tag: abc1234

sources:
  entries:
    - https://www.youtube.com/@ExampleChannel/videos

git:
  enabled: true
  remote: https://github.com/example/transcripts.git
  existingSecret: transcriber-git

persistence:
  storageClass: ceph-block
  size: 10Gi
